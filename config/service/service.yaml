apiVersion: v1
kind: Service
metadata:
  name: polis-controller
  labels:
    app.kubernetes.io/name: polis
    app.kubernetes.io/component: controller
spec:
  selector:
    app.kubernetes.io/name: polis
    app.kubernetes.io/component: controller
  ports:
    - name: http
      port: 80
      targetPort: http
